# Redis SDS（简单动态字符串）模块 📝

## 📚 模块简介

本模块包含Redis SDS（Simple Dynamic String）的实现，这是Redis String类型的底层数据结构。

**SDS是什么？**
> 一种优化的字符串结构，相比C字符串，提供O(1)长度获取、空间预分配、惰性释放和二进制安全

---

## 📂 文件列表

| 文件名 | 类型 | 说明 |
|--------|------|------|
| **SimpleDynamicString.java** | 代码 | SDS完整实现 |

---

## 🎯 为什么需要SDS？

### C字符串的问题

```c
// C字符串
char str[] = "Hello";

问题1: 获取长度需要遍历 - O(N)
strlen(str);  // 要遍历到\0

问题2: 容易缓冲区溢出
strcat(str, " World");  // 可能溢出！

问题3: 修改字符串需要重新分配
// 频繁的malloc/free

问题4: 只能存储文本
// 遇到\0就结束，不能存储二进制数据
```

### SDS的优势

```java
// Redis SDS
SimpleDynamicString sds = new SimpleDynamicString("Hello");

优势1: O(1)获取长度
int len = sds.length();  // 直接读取len字段

优势2: 避免缓冲区溢出
sds.append(" World");  // 自动扩容，安全

优势3: 减少内存分配
// 空间预分配 + 惰性释放

优势4: 二进制安全
// 可以存储图片、音频等二进制数据
```

---

## 🚀 快速开始

### 编译运行

```bash
# 编译
javac -d ../../../../../../target/classes SimpleDynamicString.java

# 运行
java -cp ../../../../../../target/classes com.architecture.principle.sds.SimpleDynamicString
```

### 代码示例

```java
// 1. 创建SDS
SimpleDynamicString sds = new SimpleDynamicString("Hello");

// 2. 获取长度 - O(1)
System.out.println("长度: " + sds.length());  // 5

// 3. 追加字符串 - 自动扩容
sds.append(" World");
System.out.println("内容: " + sds);  // Hello World

// 4. 清空字符串 - 惰性删除
sds.clear();
System.out.println("长度: " + sds.length());  // 0
System.out.println("容量: " + sds.getFree());  // 11（保留空间）

// 5. 查看内存信息
System.out.println(sds.getMemoryInfo());
```

---

## 💡 核心概念

### SDS结构

```java
public class SimpleDynamicString {
    private char[] buf;   // 字节数组（存储内容）
    private int len;      // 已使用长度
    private int free;     // 未使用长度

    // 示例：
    // "Hello" 存储为：
    // buf:  ['H','e','l','l','o','\0',' ',' ',' ',' ']
    // len:  5
    // free: 5
}
```

### 核心特性

#### 1️⃣ O(1)时间获取长度

**C字符串：**
```c
// O(N) - 需要遍历
int len = strlen(str);
```

**SDS：**
```java
// O(1) - 直接读取
int len = sds.length();  // 读取len字段
```

**优势：**
- 频繁获取字符串长度时性能显著提升
- STRLEN命令时间复杂度O(1)

#### 2️⃣ 空间预分配

**策略：**
```java
if (newLen < 1MB) {
    // 小于1MB：分配2倍空间
    allocate(newLen * 2);
} else {
    // 大于1MB：额外分配1MB
    allocate(newLen + 1MB);
}
```

**示例：**
```
原字符串: "Hello" (len=5, free=0)
追加 " World": 需要11字节

空间预分配:
- 新长度: 11字节
- 分配: 11 * 2 = 22字节
- 结果: len=11, free=11

再次追加 "!": 无需重新分配
- 使用空闲空间: free--
- 结果: len=12, free=10
```

**优势：**
- 减少内存分配次数
- 提高字符串拼接性能
- 适合频繁append的场景

#### 3️⃣ 惰性空间释放

**策略：**
```java
public void clear() {
    len = 0;         // 标记为空
    // free保持不变 // 不释放内存
}
```

**示例：**
```
字符串: "Hello World" (len=11, free=5)

执行clear():
- len变为0
- free变为16
- buf数组保留

优势：
- 避免频繁的free和malloc
- 下次使用无需重新分配
```

**适用场景：**
- 字符串频繁清空再使用
- 缓存场景
- 临时字符串操作

#### 4️⃣ 二进制安全

**C字符串：**
```c
// 遇到\0结束
char str[] = "Hello\0World";
printf("%s", str);  // 只输出"Hello"
```

**SDS：**
```java
// 使用len判断结束
SimpleDynamicString sds = new SimpleDynamicString();
sds.append("Hello\0World");  // 完整存储
System.out.println(sds.length());  // 11（包含\0）
```

**优势：**
- 可以存储任意二进制数据
- 图片、音频、序列化对象
- 不依赖特殊字符判断结束

---

## 📊 性能对比

### 操作性能对比

| 操作 | C字符串 | SDS | 提升 |
|------|---------|-----|------|
| **获取长度** | O(N) | O(1) | 100倍+ |
| **追加字符串** | O(N)每次 | O(1)均摊 | 10倍+ |
| **复制字符串** | O(N) | O(N) | 相同 |
| **比较字符串** | O(N) | O(N) | 相同 |

### 内存使用对比

```
场景：存储"Hello"（5字符）

C字符串:
- 存储: 6字节（含\0）
- 总计: 6字节

SDS:
- 存储: 10字节（预分配）
- 元信息: 8字节（len + free）
- 总计: 18字节

空间换时间：
- 多用3倍空间
- 换100倍性能提升
```

### 实际测试

```java
// 测试：追加10000次
long start = System.currentTimeMillis();

// C字符串模拟（String）
String str = "";
for (int i = 0; i < 10000; i++) {
    str += "a";  // 每次创建新对象
}

long end = System.currentTimeMillis();
System.out.println("String耗时: " + (end - start) + "ms");

// SDS
start = System.currentTimeMillis();
SimpleDynamicString sds = new SimpleDynamicString();
for (int i = 0; i < 10000; i++) {
    sds.append("a");  // 原地修改
}
end = System.currentTimeMillis();
System.out.println("SDS耗时: " + (end - start) + "ms");

// 结果：SDS快10-100倍
```

---

## 🎯 使用场景

### 场景1：频繁获取字符串长度
```java
// Redis命令：STRLEN key
// 如果用C字符串，每次都要遍历
// 用SDS，O(1)直接返回

SimpleDynamicString sds = new SimpleDynamicString("Hello World");
int len = sds.length();  // O(1)
```

### 场景2：字符串拼接
```java
// Redis命令：APPEND key value
// 空间预分配减少内存分配次数

SimpleDynamicString sds = new SimpleDynamicString("Hello");
sds.append(" ");      // 可能触发预分配
sds.append("World");  // 使用预分配的空间，无需重新分配
sds.append("!");      // 继续使用预分配的空间
```

### 场景3：缓冲区
```java
// Redis AOF缓冲区、输出缓冲区
// 频繁写入，偶尔清空

SimpleDynamicString buffer = new SimpleDynamicString();
buffer.append("command1\n");
buffer.append("command2\n");
buffer.append("command3\n");

// 写入磁盘后清空
buffer.clear();  // 惰性释放，保留空间

// 下次使用无需重新分配
buffer.append("command4\n");
```

### 场景4：存储二进制数据
```java
// 存储序列化对象、图片等
byte[] imageData = loadImage("photo.jpg");
SimpleDynamicString sds = new SimpleDynamicString();
// 存储二进制数据（包含\0）
sds.setBinary(imageData);
```

---

## 🔍 常见问题

### Q1: SDS比C字符串占用更多内存吗？
```
答案：是的，但值得

额外开销：
1. len字段：4字节
2. free字段：4字节
3. 预分配空间：最多2倍

示例："Hello"（5字符）
- C字符串：6字节
- SDS：18字节（5 + 5预分配 + 8元信息）

结论：
- 用3倍空间，换100倍性能
- Redis是内存数据库，追求速度
- 空间换时间是合理的
```

### Q2: 为什么要空间预分配？
```
问题：频繁append导致频繁内存分配

示例：
// 没有预分配
for (int i = 0; i < 1000; i++) {
    sds.append("a");
    // 每次都要：
    // 1. malloc新空间
    // 2. 复制旧数据
    // 3. free旧空间
}
// 总共：1000次malloc + 1000次free

// 有预分配
for (int i = 0; i < 1000; i++) {
    sds.append("a");
    // 只在空间不足时分配
    // 每次分配2倍空间
}
// 总共：约10次malloc + 10次free

性能提升：100倍
```

### Q3: 什么时候用惰性释放？
```
适合场景：
1. 字符串频繁清空再使用
   - 缓冲区、临时字符串

2. 字符串长度波动
   - 先长后短再长

不适合场景：
1. 字符串只用一次
   - 会浪费内存

2. 内存紧张
   - 需要及时释放

Redis的策略：
- 默认使用惰性释放
- 内存紧张时触发内存回收
```

### Q4: SDS如何实现二进制安全？
```
关键：不依赖\0判断结束

C字符串：
char str[] = "Hello\0World";
// strlen(str) = 5（遇到\0停止）

SDS：
buf:  ['H','e','l','l','o','\0','W','o','r','l','d']
len:  11
// 使用len判断长度，不关心\0

优势：
- 可以存储任何字节序列
- 支持二进制协议
- 可以存储序列化数据
```

---

## 🎓 实战练习

### 练习1：测试空间预分配
```java
SimpleDynamicString sds = new SimpleDynamicString();
System.out.println("初始: " + sds.getMemoryInfo());

sds.append("Hello");
System.out.println("追加5字符: " + sds.getMemoryInfo());

sds.append(" World");
System.out.println("追加6字符: " + sds.getMemoryInfo());

// 观察：第二次追加是否触发内存分配
```

### 练习2：比较String和SDS性能
```java
// String
long start = System.currentTimeMillis();
String str = "";
for (int i = 0; i < 10000; i++) {
    str += "a";
}
long end = System.currentTimeMillis();
System.out.println("String: " + (end - start) + "ms");

// SDS
start = System.currentTimeMillis();
SimpleDynamicString sds = new SimpleDynamicString();
for (int i = 0; i < 10000; i++) {
    sds.append("a");
}
end = System.currentTimeMillis();
System.out.println("SDS: " + (end - start) + "ms");
```

### 练习3：实现SDS扩展方法
```java
// 1. 实现substring
public SimpleDynamicString substring(int start, int end) {
    // TODO: 实现
}

// 2. 实现trim
public void trim() {
    // TODO: 去除首尾空格
}

// 3. 实现compare
public int compareTo(SimpleDynamicString other) {
    // TODO: 字符串比较
}
```

---

## 📈 性能优化建议

### 1. 合理设置初始容量
```java
// 如果知道字符串大小，预先分配
SimpleDynamicString sds = new SimpleDynamicString(1024);  // 预分配1KB

// 避免多次扩容
for (int i = 0; i < 1000; i++) {
    sds.append("data");
}
```

### 2. 批量操作
```java
// 不好：多次append
sds.append("Hello");
sds.append(" ");
sds.append("World");

// 好：一次append
sds.append("Hello World");
```

### 3. 及时释放内存
```java
// 使用完后，如果不再需要
sds.clear();
sds.compact();  // 释放多余空间
```

---

## 🔗 相关资源

### 官方文档
- [Redis Strings](https://redis.io/docs/data-types/strings/)
- [Redis SDS](https://github.com/redis/redis/blob/unstable/src/sds.h)

### 推荐阅读
- 《Redis设计与实现》- 第2章（简单动态字符串）
- [Redis源码解析 - SDS](https://github.com/redis/redis/blob/unstable/src/sds.c)

---

## 📞 总结

### SDS的核心优势
```
✅ O(1)获取长度 - 快速
✅ 空间预分配 - 减少内存分配
✅ 惰性释放 - 避免频繁free
✅ 二进制安全 - 存储任意数据
✅ 避免缓冲区溢出 - 安全
```

### 适用场景
```
✅ 字符串存储
✅ 缓冲区
✅ AOF文件
✅ 二进制数据
✅ 任何需要频繁修改的字符串
```

### 设计思想
```
用空间换时间
- 多用一点内存
- 换来更快的速度

懒惰是美德
- 惰性释放
- 减少系统调用

安全第一
- 自动扩容
- 二进制安全
```

---

**开始探索SDS的高效世界吧！** 📝

建议运行代码示例，观察空间预分配和惰性释放的实际效果！
